/** Yeni kayıt oluştururken org/global alanları için varsayılanlar */
export const ENTITY_SCOPE_DEFAULTS = {
  globalId: null,
  organizationId: null,
} as const;

export const BRANCH_SCOPE_DEFAULT = {
  branchId: null,
} as const;
